#include<stdio.h>
void main()
{
	int a,b;
	puts("enter any two numbers to check they are equal are not");
	scanf("%d %d",&a,&b);
	if(a==b)
	{
		printf("%d and  %d are equal",a,b);
	}
	else
	printf("%d and %d are not equal",a,b);
	
}
